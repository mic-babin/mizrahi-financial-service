import { Link } from "gatsby-plugin-react-i18next";
import styled from "styled-components";

export const Lang = styled(Link)`
  padding-top: 0.45rem;
  padding-right: 2rem;
  color: white;
  text-decoration: none;

  &:hover {
    color: white;
  }
`;
